Program exemplo1 ;
var
	numero:integer;
Begin
  write('informe o mumero: ' ) ;
	read(numero) ;
	
  if (numero > 0 ) then
   write('positivo') 
	else if (numero > 0 ) then
   write('negativo' ) 
	else
	write('zero' ) ;
	readkey;     
End.